pub fn raindrops(n: u32) -> String {
    match n {
        0 => String::from("0"),
        _ => {
            [(3, "Pling"), (5, "Plang"), (7, "Plong")].iter().try_fold(String::new(), |s, n| )
        }
    }
}
